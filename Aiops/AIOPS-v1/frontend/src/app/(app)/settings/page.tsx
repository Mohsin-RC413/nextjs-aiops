export default function SettingsPage() {
  return (
    <div className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-6 text-sm text-white/70">
      <p className="text-xs uppercase tracking-[0.3em] text-[var(--muted)]">Preferences</p>
      <p className="mt-1 text-2xl font-semibold text-[var(--text)]">Settings</p>
      <p className="mt-2 max-w-xl text-sm text-[var(--muted)]">
        This placeholder keeps the navigation experience consistent after the migration. Add your settings screen here.
      </p>
    </div>
  );
}
